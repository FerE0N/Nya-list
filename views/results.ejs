<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8" />
    <title>Resultados de la Encuesta</title>
    <link rel="stylesheet" href="/style.css" />
</head>

<body>
    <div class="container">
        <h1>Resultados 🏆</h1>
        <% if (results.length===0) { %>
            <p>No hay votos aún.</p>
            <% } else { %>
                <ul>
                    <% results.forEach(r=> { %>
                        <li><strong>
                                <%= r._id %>
                            </strong>: <%= r.count %> votos</li>
                        <% }) %>
                </ul>
                <% } %>
    </div>
</body>

</html>