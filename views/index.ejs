<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Encuesta Nominated Admins</title>
    <link rel="stylesheet" href="/style.css" />
</head>

<body>
    <div class="container">
        <h1>Encuesta: Nominated Admins</h1>

        <% if (!voted) { %>
            <form id="vote-form" action="/vote" method="POST">
                <% candidates.forEach(name=> { %>
                    <button type="submit" name="candidate" value="<%= name %>">
                        <%= name %>
                    </button>
                    <% }) %>
            </form>

            <div id="thank-you-message">
                <h2>¡Gracias por votar! <span role="img" aria-label="emoji de urna">🗳️</span></h2>
                <p>Los resultados se harán públicos el <strong>21/10/2025 a las 8:00 a.m.</strong></p>
            </div>

            <% } else { %>
                <div class="already-voted-message">
                    <h2>¡Ya has votado! <span role="img" aria-label="emoji de verificación">✅</span></h2>
                    <p>Mantente atento, los resultados se revelarán el <strong>21/10/2025 a las 8:00 a.m.</strong></p>
                </div>
                <% } %>
    </div>

    <script src="/script.js" defer></script>
</body>

</html>